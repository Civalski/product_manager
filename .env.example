# Copie para ".env" (mesmo diretório deste arquivo = raiz do repositório product_manager)
# ou crie backend/ProductStore.Api/.env — arquivos existentes são carregados em sequência
# e o último valor de cada chave prevalece (ver DotEnvBootstrap em Program.cs).
#
# Documentação da API: https://cosmos.bluesoft.com.br/api

Cosmos__Token=